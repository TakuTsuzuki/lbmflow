# Project Review #2 (2026-07-05 evening — point of achieving the mandatory three pillars)

Progress evaluation since the COMPETITIVE_SPEC was established (that same morning). Previous review: REVIEW_2026-07-05.md.

## 1. Verdicts on Mandatory Requirements R1-R5

| Requirement | Verdict | Evidence |
|---|---|---|
| R1 3D (D3Q19) | **Achieved** | T15: z-invariance degradation 8.9e-16 / duct exact series L∞rel 2.3e-4, flow rate 0.094% / sphere drag (normalized against the hydrodynamic pair) +7.1%/+0.6%/+2.3% / TGV3D order 1.910, decay rate 0.11%. Runnable from the CLI by specifying nz (VTK/cross-section PNG) |
| R2 GPU | **Achieved** | Wgpu backend integrated (feature `gpu`). Push-style fusion gives strict operator-order match with CPU; T14 6 configurations ≤1e-5 (pressure-only cases 1e-4, with Metal fast-math's ulp amplification demonstrated via a controlled experiment). 5,857–11,365 MLUPS |
| R3 Supercomputer | **Achieved (within single-node verification scope)** | MPI (feature `mpi`): all T13-MPI cases **bit-identical** (8-rank 3D, seam cylinder + probe, droplet with psi exchange). Weak scaling 99.4%/97.0% (2/4 ranks). Multi-node measurement is pending cluster access (8-item measurement list in MPI_GUIDE) |
| R4 Agent-native | **Achieved** | Scenario JSON (3D support in place) + 7 MCP tools (including asynchronous start_run/run_status/list_runs) + gallery |
| R5 Validation continuity | **Achieved** | The existing 2D suite passes unmodified, green, via the compat facade. 205 tests across the workspace. The equivalence framework T13/T13-MPI/T14/T15 is operational |

## 2. Performance Summary (same physics, same validation)

| Backend | 2D peak | 3D 128³ |
|---|---|---|
| CpuScalar (reference) | 314 MLUPS | 139 |
| **CpuSimd (fused)** | **1,183 MLUPS** (new CPU record) | 260 |
| **Wgpu (Metal)** | **11,365 MLUPS** | (3D GPU is next up) |

## 3. Current Architecture State

- Core V2: Lattice(D2Q9/D3Q19) x Backend(CpuScalar/CpuSimd/Wgpu) x Precision(f32/f64) x
  Decomposition(Local/InProcess/MPI) compose orthogonally. Equivalence-test coverage spans every axis
- V1 retirement work is in progress (v1-retirement branch): compat integration, wasm switch-over, renaming
- The dual-core state is temporary (will collapse to a single core once retirement completes)

## 4. Next Queue (in priority order)

1. **Complete V1 retirement** (in progress) → move to a single core
2. **Remaining M-E work**: FP16 storage (shader-f16, T16 precision freeze) / 3D GPU kernel /
   multi-GPU (Fields genericization — GPU friction point #1) / Esoteric-Pull (halves memory)
3. **Public benchmark document**: formal measurement on an idle machine + a comparison table with matched
   conditions against FluidX3D/M-Star
4. **3D cavity reference table** (T15.5, source citation required, smoothness check)
5. **Cluster measurement** (requires user consultation: HPC access) → full achievement of R3
6. Improve load balancing for the 12-thread 3D fusion (shared band-edge collision, or dynamic bands)
7. GUI: 3D cross-section view, or explicitly document the "3D is CLI/agent" positioning;
   raise the resolution ceiling (already done in the new wasm kernel)
8. Vertical features (M-F): LES → moving boundary (IBM) → scalar transport (toward M-Star's core use case)

## 5. Risks and Notes

- Absolute performance figures are reference values on this machine (shared, mixed P-core/E-core). Idle-machine
  measurement is required before any public comparison
- wgpu is f32-only — maintain the division of labor where the CPU handles f64 validation
- MCMP's MPI decomposition has only the single-component psi exchange wired up (the two-component case is
  unverified — needed before M-F)
