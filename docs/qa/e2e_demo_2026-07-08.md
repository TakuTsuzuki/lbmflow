# End-to-end bioprocess demo — 2026-07-08

## Single-phase stirred tank

- Scenario: `examples/bioprocess/stirred_tank_screening.json`
- Output: `target/e2e_demo/single_phase/`
- QOIs computed: `[Np=NaN/null, P/V=NaN/null, t95=not computed, shear P95=not computed]`
- Report: not generated; `lbm bioprocess report target/e2e_demo/single_phase/` requires `qoi.json`, and the run stopped before writing it.
- Behavior review: The dominant intended mechanism is radial Rushton forcing in a baffled closed tank, with passive scalar redistributed by the resolved velocity field. The observed behavior is not reportable: torque and scalar CV become non-finite before the QOI bundle is written, so the apparent velocity snapshots are partial pre-failure artifacts rather than a valid screening result. Resolved terms active in the attempted run were hydrodynamic LBM, IBM impeller forcing, finite-difference shear, and passive-scalar ADE; the declared cell-tracer damage model is not active in this runner path. Boundary artifacts cannot be cleared because the stress reducer receives no finite fluid-cell shear values and aborts at `crates/lbm-cli/src/runner.rs:1659`.
- Screening tier declared? NO. The scenario declares screening tier, but the run did not produce a valid screening report.

Partial artifacts written before the stop:

- `target/e2e_demo/single_phase/scenario.json`
- `target/e2e_demo/single_phase/velocity_speed_200.png`
- `target/e2e_demo/single_phase/velocity_speed_200.vtk`
- `target/e2e_demo/single_phase/velocity_speed_400.png`
- `target/e2e_demo/single_phase/velocity_speed_400.vtk`
- `target/e2e_demo/single_phase/torque_force.csv`
- `target/e2e_demo/single_phase/scalar_cv.csv`
- `target/e2e_demo/single_phase/qoi_power.csv`
- `target/e2e_demo/single_phase/qoi_power.json`

## Aerated stirred tank

- Scenario: `examples/bioprocess/aerated_stirred_tank_screening.json`
- Output: `target/e2e_demo/aerated/`
- QOIs computed: none; not run after the single-phase stop condition.
- Report: not generated.
- Behavior review: Not performed because the single-phase run produced non-finite QOI inputs and the order required stopping rather than tuning scenarios to force a green run.
- Screening tier declared? NO.
- Report banner: not available; report generation was not reached.
- Gas holdup: not computed.
- kLa: not computed.

## Findings

- The requested 48³ D/T = 1/3 stirred-tank geometry initially hit the geometry resolution hard floor: `min(impeller diameter, vessel diameter) / dx` was 15.988 while the previous default floor was 32. Screening runs are allowed to be under-resolved by `docs/SPEC_BIOPROCESS_CORE.md`, so the code now keeps 32 cells for Engineering/Evidence and adds a screening floor of 12 cells at `crates/lbm-core/src/geometry.rs:10` and `crates/lbm-scenario/src/bioprocess.rs:452`.
- Both scenarios validate with no hard rejections, but with screening warnings from the unit layer: `TAU_NEAR_HALF`, `GRID_REYNOLDS_HIGH`, `SCALAR_DIFFUSION_UNSTABLE`, and `STOKES_UNAVAILABLE`. The warning logic is in `crates/lbm-scenario/src/units.rs:227`.
- The single-phase run produced non-finite scalar CV starting at `target/e2e_demo/single_phase/scalar_cv.csv:32` and non-finite torque/force starting at `target/e2e_demo/single_phase/torque_force.csv:33`.
- `target/e2e_demo/single_phase/qoi_power.csv:3` through `target/e2e_demo/single_phase/qoi_power.csv:6` contain `NaN` for torque, power, Np, and P/V; `qoi_power.json` serializes the same invalid values as `null`.
- The run command ended with `Error: stress QOI has no fluid cells`, emitted by the stress percentile reducer at `crates/lbm-cli/src/runner.rs:1659`.
- The current bioprocess runner surface is still the single-phase path: it only requires `physics.kind = single_phase` and otherwise runs `run_bioprocess_single_phase`, as shown at `crates/lbm-cli/src/runner.rs:87`. Therefore the aerated scenario's phase field, sparger gas injection, oxygen scalar, gas holdup, and kLa declarations are not demonstrated by this run.
