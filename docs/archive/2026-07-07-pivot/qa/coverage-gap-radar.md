# V&V Coverage-Gap Radar (2026-07-07)

**Purpose.** Ranked catalog of the highest-value UNTESTED corners of the
LBMFlow V&V net, viewed after Pass-8 of the anomaly sweep. Each row is
scored on (a) physics relevance to the product (stirred tank, sparger,
particles-in-flow, LES, multiphase, GPU/MPI production) and (b) audit
signal for defect probability (open ANOMs, code-to-spec drifts, band
vacuity, pitfall-checklist verdicts). Delete a row when its sentinel lands.

**Method.** Cross-reference: `VV_MASTER_PLAN.md` axis inventory ·
`VV_TRACEABILITY.md` status column · `anomaly-log.md` open ledger ·
`pitfall-checklist.md` GAP/PARTIAL rows · `code-to-spec-diff.md` §B action
items · `multiphase-hard-cases.md` ranked case list · `mutation-coverage.md`
survivors (currently zero) · `vv-methodology-audit.md` G-series gaps.
Read-only synthesis; no test was run in this pass.

**Ranking scheme.** `Score = defect_probability × impact / cost` on a 3×3×3
band. Rows within a tier are ordered by expected first-defect signal.

---

## Ranked coverage gaps

| # | Lane / topic | Why untested (evidence) | P(defect) | Impact | Cost | Proposed sentinel |
|---|---|---|---|---|---|---|
| 1 | **Axis 1.7 — SC pressure-tensor R-dependence sweep** (extend P4-023) | P4-023 measured 13.5%→6.1% error over R∈{12,16,20} — an unclosed R-dependence that projects onto Jurin (P4-014 1.54×) and Taylor-Culick (P4-017 0.49×). The three-way σ referee is still open. `multiphase-hard-cases.md` and anomaly-log Pass-7 both flag this as the highest-value multiphase referee. No band on σ_YL(R) shape today. | **H** | **H** — every SC production claim on curved menisci or dynamic interfaces depends on it (sparger #9.1, coalescence, Bretherton, Bhaga-Weber). | **S** | Extend `accuracy_audit_sc_pressure_tensor.rs` with R ∈ {8, 12, 16, 20, 28, 40}; fit σ_YL(R) = σ_∞ + c/R^n, freeze n within ±0.2, cross-plot against Jurin measurement using the R-dependent σ; expected mechanism decider. |
| 2 | **Axis 1.7 — Bouzidi MOVING walls** | P.4-011 kill-case queue open (anomaly-log OPEN — audit-side revision). PHYSICS.md claim "2nd-order for curved walls" is silently 1st-order in 3 of 4 branches (code-to-spec A9). No moving-Bouzidi test exists (`bouzidi.rs` uses static rim). Rotating cylinder / stirred impeller with curved surface is a headline product target. | **H** | **H** — stirred-tank blade tip curvature is the direct downstream user. Failure hides as loose Cd/Np bands. | **M** | New `accuracy_audit_bouzidi_moving.rs`: rotating cylinder around off-grid center, 3 D∈{20,40,80}, measure force and slip vs analytic Stokes / Oseen; also verify branch-mix statistics (per-link report of {qd<1/2 with 2nd node, qd<1/2 halo-edge, qd≥1/2}) so the doc validity clause can be written. |
| 3 | **Axis 1.7 — FP16/deviation storage analytic error model** | T16 heavy is BENCH-PENDING per traceability; `t16_fp16_storage.rs` bands are frozen to one machine measurement, not to a derived model. Deviation-storage f32 numbers (whitepaper claim 14) are PARTIAL in the claims cross-check — "no single named test asserts both the 1.34e-3→2.8e-7 numbers as regression pins." Precision-audit lane 7.3 is unstarted. | M | **H** | **S** | Derive theoretical rounding-error bound for `f_dev = f - w` over N steps under TGV/uniform-force; add a small `t16_storage_error_model.rs` cross-checking measured drift vs derived envelope for both f32 and f16 at 3 magnitudes (small |u|=0.01, mid 0.05, near-Ma 0.15). |
| 4 | **Axis 3.4 — Post-P4-001 fix-design review panel** | P4-001/010/021/022 all closed with core-side patches; no independent judge panel ran on the fix designs before the gates were declared green. VV_MASTER_PLAN §3.4 says "GATE (P4-001/010 core landing)" — the P4-001 gate has landed but the review panel is not recorded anywhere. Fix designs (row-sum overlap mobility for IBM; raw-Guo-momentum Zou-He; additive SC field) are load-bearing. | M | **H** — any residual defect propagates through every downstream stirred-tank / multiphase gate. | S | 2-3 subagent judges (Sonnet/Opus mix) each derive the corrected discretization independently and diff against `rotating_ibm.rs::apply_rotating_ibm`, `kernels.rs::zou_he_face_selected/_3d/_d3q27`, and `compat/multiphase.rs::ShanChen::update_force`; disagreements land as anomaly rows. |
| 5 | **Axis 5.2 — Randomized property (proptest) sweep** | Never dispatched (W2 in master plan). Interaction-matrix lane 5.1 already surfaced two silent bugs (P4-021 mass leak + P4-022 SC overwrite) that single-mutation coverage cannot reach. Property invariants (conservation, boundedness, determinism, mirror equivariance) untested outside frozen scenarios. | **H** | **H** — pair-only defect class (P4-022 family) is invisible today except through the hand-authored matrix. | **M** | Add `proptest` dev-dep; test `arbitrary_legal_scenario` → invariants: (i) mass drift ≤ N·ε; (ii) |u|_max monotone in tau below a threshold; (iii) 90°-rotation output equals rotated input; (iv) rerun with same seed = bit-identical. Cap at 200 generated configs per test with shrinking. |
| 6 | **Axis 5.5 — Stability-envelope cartography (per collision op)** | W3 in master plan — unstarted. ANOM-P1-003 (silent max|u|>0.15) and A1 pass-1 monitoring (out-of-envelope stirred stays "STABLE") are open on the same axis. Users hit divergence with no diagnostic. | M | **M** — production honesty; `tune-stability` skill relies on this map. | M | Automated (tau, Ma, grid-Re) sweep script under `scripts/qa/stability_envelope.py`; per collision {BGK, TRT Λ=3/16, CentralMoment} produces a JSON envelope + PNG heat-map; freeze first envelope, monitor for regressions on the corners. |
| 7 | **Axis 5.3 — Visual anomaly trawl (scripted × qa-viewer)** | W2 unstarted. Sedimentation experiment (Axis 9.4) already demonstrated that a band can pass while the pattern is a boundary artifact (rev-2 mean_dep_x=500 out-of-domain). No systematic PNG/VTK production for the standard scenario matrix. | M | **H** — the behavior-validity directive explicitly requires pattern-level review; this is the enforcement lane. | M | Scripted sweep of {cavity, cylinder-karman, droplet, droplet-on-wall, sedimentation-basin, sparger-analog} × 4 param variations, produce PNGs + `sim-anomaly-scan` reports into `out/qa/visual-trawl/`, chip PM review each. |
| 8 | **Axis 6.3 — D3Q19 GPU direct absolute physics** | Traceability marks T14-D3 as VERIFIED-ONLY (CPU-vs-GPU relative). Backend-precision audit §"Relative-Only" flags D3Q19 GPU absolute sentinels as still needed. Every 3D production run (VR-STR, stirred tank) will land on this backend path. A shared CPU/GPU physics bug is invisible today. | **H** | **H** — the primary 3D production surface. | M | Extend `t14_gpu_absolute_physics.rs` (D2Q9 landed) with D3Q19 direct: TGV3D order + effective-nu; rectangular duct series L∞rel; Ghia-3D Re=100 profile; conservation ledger. Feature-gpu, ignored heavy. |
| 9 | **Axis 6.3 — D3Q19 GPU Ghia + conservation sentinels** | Same audit lists these as remaining. Ghia-2D GPU-direct absent even at D2Q9 (backend-audit lists it as follow-up). Direct GPU mass/momentum conservation across 3D pass order is unmeasured. | M | **H** | M | Add GPU direct Ghia Re=400 2D (band ties to T7); GPU 3D conservation ledger (mass drift + momentum growth under uniform force) on N=64³ D3Q19. |
| 10 | **Axis 7.3 — SIMD `unsafe` UB + f32↔f64 cast audit** | W3 in master plan — unstarted. `backend_simd` uses `unsafe` for AVX2/NEON loads; no MIRI or unaligned/edge test. Kahan-summation absence in reductions is unaudited but T6-f32 momentum drift band (1e-5) may be masking it. | M | **M** — silent-wrong risk on non-macOS AVX2 hosts. | M | `cargo +nightly miri test -p lbm-core --test smoke_conservation --test backend_simd_equiv` on scalar path; audit every `unsafe` block for provenance + alignment; add `f32_reduction_kahan_vs_naive.rs` cross-check on a 1024²×1024-step accumulation. |
| 11 | **Axis 8.2 — WASM/GUI parity vs native** | W3 unstarted. The web GUI is committed and the paper claim (row 27) verifies structurally, but no parity gate compares WASM step trajectories to native. Any WASM-only precision or ordering drift is silent. | M | **M** — user-facing surface. | M | New `web/tests/parity.spec.ts` (Playwright + native harness): run cavity + cylinder-karman via WASM for N steps, dump density/velocity, compare to native `lbm run` output with the same seed, band ≤ 1e-5 rel (f32 class). |
| 12 | **Axis 8.3 — CLI/MCP contract tests (schema/manifest/VTK)** | W3 unstarted. VV_SCENARIO_CLI_AUDIT.md closes only the `inletProfile` gap. MCP tools (`run_scenario`, `start_run`, `run_status`, `list_runs`, `validate_scenario`, `list_presets`, `get_schema`) have no round-trip schema tests. Manifest / VTK formats undocumented as gate. | M | **M** — regressions ship as silent output-schema drift. | S | Golden-file tests in `crates/lbm-cli/tests/contract.rs`: (i) `lbm presets show cavity` byte-diff; (ii) preset run → assert manifest fields present + types; (iii) MCP schema round-trip; (iv) VTK header + field-count invariants. |
| 13 | **Axis 8.4 — Units V&V extension** | W3 unstarted. `lbmflow-user-scaleup-advisor` skill and REQ_STIRRED_REACTOR.md both operate in SI; scenario input is lattice units; no gate that a SI→lattice round-trip preserves a specified physical result. | M | **M** — VR-STR-01 will hit this the moment SI-authored scenarios are added. | S | `crates/lbm-cli/tests/units_round_trip.rs`: given a SI scenario spec (D=10 cm, N=5 rev/s, water @ 20°C), round-trip through the units layer; assert Re, tip speed, ν_lu recovered within 1e-9; add cross-check that at least one of {Poiseuille, Couette} preserves quantitative agreement across the round-trip. |
| 14 | **T13-MPI runtime validation** | BENCH-PENDING per traceability. `./scripts/test_mpi.sh` blocked by PRTE socket-bind in every session so far. Whitepaper claim #28 (64-rank weak-scaling ≥80%) is UNPROVEN. The cluster runbook exists but has never gated a landing. | **H** | **H** — production scale-out. | M | Fix the sandbox: run on a bare-metal / SLURM host from CLUSTER_OPTIONS.md; commit the resulting `mpi_t13` bit-match report + a `bench_mpi.sh` weak-scaling artifact tied to whitepaper §7. Requires user-side host. |
| 15 | **P5 checkerboard-mode decay** (pitfall #5, the sole "GAP") | Never tested. The one true GAP in the pitfall checklist. Staggered `(−1)^(x+y)` mode decay is analytic (Zanetti 1989); silent growth would corrupt every low-Ma steady state without tripping any current gate. | M | **H** | **S** | New `tests/accuracy_audit_modes.rs`: seed `rho = 1 + 1e-4·(−1)^(x+y)`, u=0, BGK and TRT at τ ∈ {0.51, 0.6, 1.0}; project ρ onto the mode each step; assert monotone decay at collision-spectrum rate, no leakage into (π,0)/(0,π) above 1e-12. 64², ≤500 steps. |
| 16 | **P10 co-moving-frame zero wall force (Wen 2014)** | pitfall-checklist PARTIAL. Momentum-exchange probe uses conventional Ladd form; static probes safe (audit-probe A2 pass) but frame-dependence never asserted. Any moving reference-frame claim (stirred tank body-frame, moving object drag) is silently wrong today. | **H** | **H** — Wen-2014 decider; feeds Bouzidi/probe convention doc lock. | **S** | Periodic box, uniform u0, both walls MovingWall at u0; assert `sim.probed_force().iter().all(|f| f.abs() < 1e-14)` after one step. |
| 17 | **P25 single-delta streaming + OPP[] table test** | pitfall-checklist PARTIAL. `stream_contract.rs` covers faces but no OPP-table sanity or single-delta propagation unit. A silent OPP[]/weight-table swap on a future lattice extension is invisible. Also validates the storage-order contract SIMD/GPU fusions depend on. | M | **H** — sub-second cost, catches large blast radius. | **S** | `tests/lattice_opp_table.rs`: for D2Q9/D3Q19/D3Q27, assert `c[OPP[q]] == -c[q]`, `w[OPP[q]] == w[q]`; inject δ at one cell/direction, stream once (no collide); assert arrival exactly at `x + c_q` with zero elsewhere. Milliseconds. |
| 18 | **P11 Bouzidi mass-drift pin** | pitfall-checklist PARTIAL. No total-mass assert with Bouzidi active. Closed off-grid geometry + long horizon + Bouzidi = the exact combination the D-track / VR-STR-01 impeller uses. | M | **H** | M | Closed periodic-x channel + off-grid cylinder, 10⁴ steps; freeze `|ΣΔρ| / Σρ` band vs staircase reference (expected small but nonzero; bound and monitor). |
| 19 | **P20 SC coexistence τ-sweep (forcing-scheme dependence)** | pitfall-checklist PARTIAL — "no τ-sweep pin of the dependence surface". P4-023 verdict candidate says "not σ but momentum coupling" — the τ-sweep will bracket which scheme surface (Guo/shift/He-Doolen) is realized. | M | **H** | **S** | Extend `validation_multiphase.rs`: flat interface at τ ∈ {0.6, 1.0, 1.4}, assert coexistence-density shift within a frozen band; Maxwell-rule deviation logged as characterization (per anomaly-log G7 discipline). |
| 20 | **P3 init-ringing amplitude comparison (TGV flat-ρ vs pressure-consistent)** | pitfall-checklist PARTIAL. T1 uses pressure-consistent init but the acoustic-mode amplitude ratio vs theory is untested. Any real user init from a scenario JSON is FLAT — silent init transient never gated. | M | M | **S** | `tests/accuracy_audit_init_ringing.rs`: TGV with two inits; project onto k-mode; ratio matches O(u0)/O(u0²) theory within 20%; freeze envelope of the flat-init acoustic overshoot. |
| 21 | **P1 force-driven vs pressure-driven Poiseuille equivalence** | pitfall-checklist PARTIAL — "MISSING: force-driven vs pressure-driven Poiseuille equivalence". P4-021 (Zou-He mass leak) is now closed; this cross-forcing-convention detector should land right after to certify the two paths agree. | M | **H** — closes the mixed-forcing pitfall (#1) that ANOM-P2-001 lives in. | **S** | Add to `validation_channel.rs`: same Re Poiseuille via (a) uniform body force, (b) pressure Zou-He at Δp; assert center-line velocity and mass-flux agreement within 1e-6 rel at H=32. |
| 22 | **P18 D3Q19 vs D3Q27 rotated-flow anisotropy discriminator** | pitfall-checklist PARTIAL — "no D3Q19-vs-D3Q27 anisotropy discriminator". P4-023 residual and Bouzidi g6 SPEC-GAP both suspect axis anisotropy on D3Q19; no direct test today. Rotor / stirred-tank tip flow may excite it. | M | M | M | 3D solid-body rotation initial condition (u = Ω×r) on D3Q19 and D3Q27, compare decay-rate anisotropy vs pure viscous decay after rotating the axis 45°; band on the D3Q19-vs-D3Q27 discrepancy. |
| 23 | **P21 WALE pure-shear ν_t ≡ 0 property test (field-level, not integral)** | pitfall-checklist PARTIAL — "verify wale_null scope covers the field, not only integral behavior". Cell-by-cell null in pure shear untested; first fluid cell near wall is the classic failure point. | M | M | **S** | Extend `wale_les.rs`: for a laminar Couette / Poiseuille field, assert `nu_t[cell]` ≤ machine ULP at every cell including the first fluid cell adjacent to bounce-back walls. |
| 24 | **Prosperetti damped interfacial standing wave (K3, T12-adjacent)** | `multiphase-hard-cases.md` rank 2 — highest info/compute of the K3 hard cases; NOT in the current in-flight `cx/mp-dynamics` set. Tests viscous transient dispersion + gravity-capillary crossover simultaneously; exact solution. | **H** | **H** — decisive for any dynamic-interface claim. | M | New `validation_prosperetti.rs`: reuse T12 rig, seed a standing k-mode, fit ω and damping; k-scan across k_σ crossover; three k values; bands 5% frequency / 10% damping. |
| 25 | **T17 stirred-reactor VR-STR-01 (Np + Costes-Couderc)** | Traceability: `SPEC-ONLY`. This IS the M-F product headline — the only reason V&V exists at this depth. Every P4-001/010/022 fix was in service of it. Its absence at "planned" is the largest single traceability gap. | M | **H** — flagship. | **L** (large) | Land VR-STR-01 harness under `crates/lbm-core/tests/validation_stirred.rs`: Rushton at Re > 10⁴, Np within ±15% and Costes-Couderc radial-jet lines. Ignored heavy; a separate order in the codex dispatch. |
| 26 | **T18.4 forward-model trend anchors (dispersed deposition)** | Traceability: `SPEC-ONLY`; anomaly-log flags "ad-hoc inventory warns earlier P1.1 bands measured through example-local closures must not be generalized". Behavior-validity risk explicitly named in CLAUDE.md as origin of the physics-discipline directive. | M | M | M | Move CLI-example anchors into a proper `t18_4_forward_model.rs` with derived (not measurement-frozen) trend bands; publish the closure ledger (interior-clamps / side-wall clamp / ...) as a REGRESSION_PIN vs VALIDATION_GATE per G7. |
| 27 | **Particles near-wall sampler bias + position clamp** (code-to-spec A12) | Trilinear sampler zeros solid contributions but does NOT renormalize corner weights (`particles.rs:358-362`); `bracket` silently clamps out-of-grid positions. Axis 9.4 rev-2 STOP-rule was triggered exactly by this. No test on the near-wall interpolation bias. | M | **H** — near-wall deposition (VR-STR-04, sedimentation, sparger particles) is the primary product target. | **S** | `tests/particles_nearwall_bias.rs`: place particle at fractional offset from a wall in a known u(y) field; assert sampled u = analytic within (1 − partial-mass)/2 (i.e. bound the bias explicitly, not silently). Freeze band. |
| 28 | **Gravity double-count trap** (code-to-spec A25) | `KParams::force_at` and `Solver::run_staged_step` both add `ρ·g`; dispatch is capability-gated but not type-state-enforced. A future backend advertising `supports_gravity_body_force=true` but not consuming `p.gravity` would silently lose gravity. Untested cross-backend. | L | **H** | **S** | `tests/gravity_dispatch_invariant.rs`: assert for every backend capability combination that `hydrostatic_column_height == analytic ± ε`; also `debug_assert!` in solver.rs:1690 that the two paths are mutually exclusive. |
| 29 | **Silent ω_shear `min(2.0)` clamp** (code-to-spec A4) | `kernels.rs:404` hard-clamps `os = ...min(2.0)` with no diagnostic. CentralMoment / WALE user paths can silently be off-band. | L | M | **S** | Add `ClampDiagnostics { omega_shear_clamped_cells: usize, max_omega_shear_before_clamp: f64 }` and wire into the manifest; test that a scenario forcing ω>2 records nonzero clamp count and emits a runtime warning. |
| 30 | **Convective outflow reflection coefficient curve (audit-side revision)** | Axis 1.7 lane; recent T9b rev-1 STOP-ruled itself as rest-frame setup mismatch. Rev-2 with mean-flow setup not yet landed. Current default `Outflow` R=0.26 vs `ConvectiveOutflow` R≈0.998 in the rest-frame test — the physical operating regime is not gated. | M | M | M | Rev-2 `validation_outflow.rs`: uniform-inlet channel + u_conv centered on u_in; sweep u_conv ∈ [0.5, 1.5]×u_in; freeze R(u_conv/u_in) curve, expect minimum near 1.0. |
| 31 | **Per-cell omega asymmetry latent defect** (code-to-spec A28) | TRT with per-cell ω: `cp` recomputed per cell but `cm` frozen to global `p.cm` (kernels.rs:182-186). Only bites future ω⁻(x) — but WALE already varies ω⁺ and any future TRT-WALE combo would hit this. No test guard. | L | M | **S** | Compile-time debug_assert! or a `tests/kernel_per_cell_omega.rs` that instantiates a TRT run with a non-trivial `omega_m`, sets per-cell ω⁺ field, and requires either (a) equal cm application, or (b) a hard error until the pair is extended. |
| 32 | **Cross-solver comparison (OpenLB @ ~/projects/cfd-bench)** — Axis 6.1 | W2 unstarted. OpenLB is already built on the host per user memory. No same-scenario cross-solver reference for cavity/cylinder/TGV committed to the repo. External referee is the last shield against a shared CPU/GPU/backend blind spot. | M | **H** | M | `scripts/qa/cross_solver.sh`: run cavity Re=100/400, cylinder Re=100, TGV order in both; produce `out/qa/cross-solver/comparison.json` and a report page; freeze paired-mean-difference band. |
| 33 | **T15 D3Q27 full physical suite (partial today)** | Traceability item #8: "D3Q27 coverage is partial. D3Q27 has smoke/TGV/partition evidence, but not the full D3Q19 T15 physical suite." Backend-precision audit: "no broad D3Q27 physical validation matrix". | M | M | M | Port T15.2 (duct), T15.3 (sphere drag), T15.5 (cavity) minimal cases to D3Q27; feature-gate the heavy ones. |
| 34 | **T17-06 well-balanced static stratification (VERIFIED-ONLY)** | Traceability: currently VERIFIED-ONLY. Only partial `gravity.rs::vr_str_06_...` sentinel. Full active-scalar degeneracy and hot/cold layer balance untested. Feeds every buoyancy-driven stirred-tank scenario. | M | M | M | Extend to a proper VR-STR-06 gate: two-layer stable stratification 10⁴ steps, quiescent to ε; passive-scalar degeneracy under uniform g; active-scalar Boussinesq analog. |
| 35 | **MCMP + per-component gravity closed-box protocol (spec-side of P4-016)** | anomaly-log Pass-8: "keep documented-red pin, revise spec in next mp-hard rev (option b — Boussinesq-analog with per-component gravity adjusted so net = 0 at t=0)". Spec revision blocked, blocks Axis-9.1 sparger path partially. | M | M | M | Draft VR-STR spec revision + a `val_mphard_i3b_net_zero_body_force_rt.rs` under the corrected protocol; band on cutoff k_c. |
| 36 | **`compat/multiphase.rs` ρ positivity guard** (code-to-spec A22) | `Psi::Exponential(ρ≤0) = exp(±∞)`; no floor. Under-resolved SC dynamics can silently overflow. No test covers the negative-ρ regime. | L | M | **S** | `tests/sc_negative_rho_guard.rs`: force ρ→ε in a controlled small test, assert Psi::Exponential returns a finite result or a hard `debug_assert!` fires; add corresponding validity clause to PHYSICS.md. |
| 37 | **CS-EOS helper missing from compat facade** (code-to-spec A16/A17) | PHYSICS.md §1 SCMP entry mentions "Carnahan-Starling (CS) EOS helpers"; `compat/multiphase.rs::Psi` enum only has `Classic` and `Exponential`. Documentation claim not backed. | L | L | **S** | Either wire the CS helper through the facade with a T11-style coexistence test, or remove the claim from PHYSICS.md §1. |
| 38 | **Long-horizon soak (Axis 5.4)** | W2 unstarted. 10⁶-step runs of {cavity, channel+cylinder, SC droplet, post-fix penalized-blade rotor} with drift ledgers absent. `d5_long_horizon.rs` covers TGV/cavity to shorter horizons; nothing pins the post-P4-001 rotor path at long horizons. | M | M | **L** | Nightly / weekly cron job runs the 4 configurations to 10⁶ steps and diffs the drift ledgers against frozen curves. |

---

## Coverage-map summary

- **Master-plan lanes still unopened**: 3.4 review-panel · 5.2 randomized · 5.3
  visual-trawl · 5.4 long-horizon · 5.5 stability-envelope · 6.1 cross-solver ·
  6.3 GPU absolute (D3Q19) · 7.3 unsafe/precision · 8.2 WASM parity · 8.3
  CLI/MCP contract · 8.4 units. Axis 1.7 has 3 items landed (SC pressure
  tensor, WALE null field-level pending as row 23, Bouzidi moving pending as
  row 2, convective-outflow curve pending as row 30, FP16 error model pending
  as row 3, tracer-limit BBO GATE-Phase-B).
- **Anomaly-log open**: P4-023 (SC σ referee, characterization), P4-016 (i3
  spec-side), P4-018/019 (SC dynamic wetting), P4-024 (thin-shell axial-mode
  filtering). Rows 1 and 35 are the direct sentinels.
- **Pitfall-checklist gap → new tests**: rows 15 (P5), 16 (P10), 17 (P25),
  18 (P11), 19 (P20), 20 (P3), 21 (P1), 22 (P18), 23 (P21) — all 9
  proposed kill-cases from `pitfall-checklist.md` §"Proposed new kill-cases".
- **Code-to-spec drifts (S2 or with live blast radius)**: rows 27 (A12),
  28 (A25), 29 (A4), 31 (A28), 36 (A22), 37 (A16/A17).
- **Traceability high-risk gaps 1-10** mapped: #1 T17 → row 25; #4 T13-MPI →
  row 14; #8 T15 D3Q27 → row 33; #9 T9/T9b horizons → row 30 (rev-2); #10
  scenario/CLI T1 init-analytic → part of row 12 contract tests + row 20.

## Priority sequencing (for the next dispatch wave)

Wave-A (S-cost, H×H tier): 1, 4, 15, 16, 17, 21, 27.
Wave-B (M-cost, H-impact): 2, 5, 8, 24, 32.
Wave-C (structural, gate on infra): 6, 7, 14, 25.
Rows 28, 29, 31, 36, 37 are cheap "opportunistic" fixes bundled with any
adjacent order.

Every row leaves a visual artifact per the master-plan principle;
regression-pin discipline (G7 — `REGRESSION_PIN` vs `VALIDATION_GATE`)
applies to every band frozen from first-measurement.
